imp commands for githubs:

** use these commands to push your files on github repo.

1. git init
2. git status
3. git add . or git add file name to add
4. git commit -m "name the commit"
5. git branch -M main
6. git remote add origin and paste the link from code
7. git push -u origin branch name


**done Thanks.